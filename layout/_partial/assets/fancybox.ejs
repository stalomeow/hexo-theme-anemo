<%# Argument: assets(object) %>

<%
// 尽量每页都有 fancybox，除非特别在 frontmatter 里指定了 false
if (theme.fancybox.enable && page.fancybox !== false) {
  assets.fancybox = [
    url_join(theme.static_prefix.fancybox, 'fancybox.css'),
    url_join(theme.static_prefix.fancybox, 'fancybox.umd.js')
  ];
}
%>